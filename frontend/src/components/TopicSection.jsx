import ImageWithFallback from './ImageWithFallback';
export default function TopicSection({ topics = [] }) { return <section className="section topic-section" id="explore"><div className="standard-heading"><p>EXPLORE BY TOPIC</p><h2>Find what inspires you.</h2></div><div className="topics">{topics.map((topic) => <a className="topic" href="#community" key={topic.name}>{topic.image && <ImageWithFallback src={topic.image} alt=""/>}<span>{topic.name}</span></a>)}</div></section>; }
