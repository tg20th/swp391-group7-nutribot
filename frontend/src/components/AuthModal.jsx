import { useEffect, useState } from 'react';
import { X } from 'lucide-react';
export default function AuthModal({ mode, onClose, onSubmit, onGoogle }) {
  const [form, setForm] = useState({ name: '', email: '', password: '' }); const isSignup = mode === 'signup';
  useEffect(() => { const close = (e) => e.key === 'Escape' && onClose(); window.addEventListener('keydown', close); return () => window.removeEventListener('keydown', close); }, [onClose]);
  return <div className="modal-backdrop auth-backdrop" role="dialog" aria-modal="true" aria-labelledby="auth-title" onMouseDown={(e) => e.target === e.currentTarget && onClose()}><form className="auth-modal" onSubmit={(e) => { e.preventDefault(); onSubmit?.(form, mode); }}><button className="modal-close" type="button" onClick={onClose} aria-label="Close"><X size={19}/></button><p>Welcome to NutriBot</p><h2 id="auth-title">{isSignup ? 'Make nourishment personal.' : 'Welcome back.'}</h2><span>{isSignup ? 'Create an account to save ideas, videos and recipes.' : 'Sign in to continue your healthy routine.'}</span><button className="google-auth" type="button" onClick={onGoogle}><b className="google-mark">G</b><span>Continue with Google</span></button><div className="auth-divider"><span>or use email</span></div>{isSignup && <label>Full name<input required value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} placeholder="Your name"/></label>}<label>Email<input required type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} placeholder="you@example.com"/></label><label>Password<input required minLength="6" type="password" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} placeholder="At least 6 characters"/></label><button className="auth-submit" type="submit">{isSignup ? 'Create account' : 'Log in'}</button><button className="auth-switch" type="button" onClick={() => onSubmit?.(null, isSignup ? 'login' : 'signup')}>{isSignup ? 'Already have an account? Log in' : 'New here? Create an account'}</button></form></div>;
}
