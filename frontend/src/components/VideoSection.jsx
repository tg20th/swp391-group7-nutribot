import { Play, VideoOff } from 'lucide-react';
export default function VideoSection({ onPlay, videos = [] }) { return <section className="section video-section" id="videos"><div className="standard-heading"><p>WATCH & LEARN</p><h2>Short videos for a healthier, happier you.</h2></div><div className="video-grid">{videos.map((video) => <article className="video-card" key={video.id}><button className="video-thumb" onClick={() => onPlay(video)} aria-label={`Play ${video.title}`}>{video.image ? <img src={video.image} alt=""/> : <span className="video-placeholder"><VideoOff size={28}/><span>Video preview</span></span>}<span className="play-overlay"><Play fill="currentColor" size={24}/></span>{video.duration && <em>{video.duration}</em>}</button><h3>{video.title}</h3><p>{video.author} {video.duration && <span>{video.duration}</span>}</p></article>)}</div></section>; }
