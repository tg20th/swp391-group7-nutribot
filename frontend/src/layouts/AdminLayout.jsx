import { Menu } from 'lucide-react'; import { Link, Outlet, useNavigate } from 'react-router-dom'; import { useState } from 'react'; import AdminSidebar from '../components/admin/AdminSidebar';
export default function AdminLayout() { const [open, setOpen] = useState(false); const navigate = useNavigate(); const logout = () => { localStorage.removeItem('nutribot-dev-role'); navigate('/'); }; return <div className="admin-shell"><AdminSidebar open={open} onClose={() => setOpen(false)} onLogout={logout}/>{open && <button className="admin-sidebar-scrim" aria-label="Close navigation" onClick={() => setOpen(false)}/>}<div className="admin-main"><header className="admin-topbar"><button className="sidebar-trigger" aria-label={open ? 'Close navigation' : 'Open navigation'} aria-expanded={open} onClick={() => setOpen(value => !value)}><Menu/></button><Link className="admin-top-brand" to="/admin" aria-label="NutriBot admin home"><span>Nutri</span>Bot<small>Good Food. Brighter You.</small></Link></header><main className="admin-content"><Outlet/></main></div></div>; }
